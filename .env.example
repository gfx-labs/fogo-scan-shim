# Fogoscan API (no auth required)
FOGOSCAN_API_URL=https://api.fogoscan.com

# Public RPC URL for proxying non-handled calls
PUBLIC_RPC_URL=https://rpc.fogo.io

# Port for the shim to listen on
PORT=8899
